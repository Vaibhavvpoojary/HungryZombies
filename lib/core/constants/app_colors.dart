import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand Colors
  static const Color primary = Color(0xFFC62828);
  static const Color primaryLight = Color(0xFFEF5350);
  static const Color secondary = Color(0xFFFFB300);

  // Background
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Colors.white;

  // Text
  static const Color textPrimary = Color(0xFF1F2937);
  static const Color textSecondary = Color(0xFF6B7280);

  // Status
  static const Color success = Color(0xFF43A047);
  static const Color error = Color(0xFFE53935);

  // Food Indicators
  static const Color veg = Color(0xFF2E7D32);
  static const Color nonVeg = Color(0xFFC62828);

  // Tags
  static const Color popular = Color(0xFFFFC107);
  static const Color fastMoving = Color(0xFF29B6F6);

  // Misc
  static const Color divider = Color(0xFFE5E7EB);
}